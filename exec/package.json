{
  "dependencies": {
    "amqplib": "^0.5.1",
    "async": "*",
    "underscore": "*"
  },
  "scripts": {
    "start": "AMQP_URL=amqp://172.20.0.1 forever start -w exec.js && forever logs -f exec.js",
    "stop": "forever stop exec.js"
  }
}
